<Window x:Class="SecureCloud.Desktop.SimpleMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SecureCloud - Encrypted Cloud Storage" 
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Style="{DynamicResource ModernWindowStyle}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Simple Toolbar -->
        <ToolBar Grid.Row="0" Margin="5" Style="{DynamicResource ModernToolBarStyle}">
            <Button Content="Test" Click="TestButton_Click" Style="{DynamicResource ModernButtonStyle}"/>
            <Button Content="Initialize" Click="InitializeButton_Click" Style="{DynamicResource ModernButtonStyle}"/>
            <Separator/>
            <Button Content="Add File" Click="AddFileButton_Click" Style="{DynamicResource ModernButtonStyle}"/>
            <Button Content="Add Folder" Click="AddFolderButton_Click" Style="{DynamicResource ModernButtonStyle}"/>
            <Button Content="View Versions" Click="ViewVersionsButton_Click" Style="{DynamicResource SecondaryButtonStyle}"/>
            <Button Content="Analytics" Click="ViewAnalyticsButton_Click" Style="{DynamicResource SecondaryButtonStyle}"/>
            <Button Content="Settings" Click="SettingsButton_Click" Style="{DynamicResource SecondaryButtonStyle}"/>
            <Separator/>
            <Button Name="ThemeToggleButton" Click="ThemeToggleButton_Click" Style="{DynamicResource ThemeToggleButtonStyle}"/>
        </ToolBar>

        <!-- Main Content -->
        <TabControl Grid.Row="1" Margin="5" Style="{DynamicResource ModernTabControlStyle}">
            <TabItem Header="Files">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- File List -->
                    <DataGrid Grid.Row="0" Name="FilesDataGrid" 
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              Margin="5"
                              Style="{DynamicResource ModernDataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="File Name" Binding="{Binding FileName}" Width="*"/>
                            <DataGridTextColumn Header="Size" Binding="{Binding SizeFormatted}" Width="100"/>
                            <DataGridTextColumn Header="Encrypted Size" Binding="{Binding EncryptedSizeFormatted}" Width="120"/>
                            <DataGridTextColumn Header="Encrypted" Binding="{Binding IsEncrypted}" Width="80"/>
                            <DataGridTextColumn Header="Uploaded" Binding="{Binding UploadedAt}" Width="150"/>
                            <DataGridTextColumn Header="Message ID" Binding="{Binding MessageId}" Width="100"/>
                            <DataGridTemplateColumn Header="Actions" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Download" 
                                                Click="DownloadButton_Click"
                                                Tag="{Binding}"
                                                Style="{DynamicResource SecondaryButtonStyle}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <!-- Status Text -->
                    <TextBlock Grid.Row="1" Name="FileStatusText"
                               Text="No files uploaded yet. Click 'Add File' to upload your first file."
                               HorizontalAlignment="Center" 
                               Margin="10"
                               Style="{DynamicResource SecondaryTextStyle}"/>
                </Grid>
            </TabItem>
            
            <TabItem Header="Folder Sync">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Folder Sync Toolbar -->
                    <ToolBar Grid.Row="0" Margin="5" Style="{DynamicResource ModernToolBarStyle}">
                        <Button Content="Add Folder" Click="AddFolderButton_Click" Style="{DynamicResource ModernButtonStyle}"/>
                        <Button Content="Remove Folder" Click="RemoveFolderButton_Click" Style="{DynamicResource SecondaryButtonStyle}"/>
                        <Separator/>
                        <Button Content="Scan All" Click="ScanAllFoldersButton_Click" Style="{DynamicResource SecondaryButtonStyle}"/>
                        <Button Content="Pause All" Click="PauseAllFoldersButton_Click" Style="{DynamicResource SecondaryButtonStyle}"/>
                        <Button Content="Resume All" Click="ResumeAllFoldersButton_Click" Style="{DynamicResource SecondaryButtonStyle}"/>
                    </ToolBar>
                    
                    <!-- Synced Folders List -->
                    <DataGrid Grid.Row="1" Name="SyncedFoldersDataGrid" 
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              Margin="5"
                              Style="{DynamicResource ModernDataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Status" Binding="{Binding StatusIcon}" Width="60"/>
                            <DataGridTextColumn Header="Folder Name" Binding="{Binding FolderName}" Width="*"/>
                            <DataGridTextColumn Header="Path" Binding="{Binding FolderPath}" Width="300"/>
                            <DataGridTextColumn Header="Files" Binding="{Binding FileCount}" Width="80"/>
                            <DataGridTextColumn Header="Size" Binding="{Binding TotalSizeFormatted}" Width="100"/>
                            <DataGridTextColumn Header="Last Sync" Binding="{Binding LastSyncFormatted}" Width="150"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="150"/>
                            <DataGridTemplateColumn Header="Actions" Width="200">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Pause" 
                                                    Click="PauseFolderButton_Click"
                                                    Tag="{Binding}"
                                                    Style="{DynamicResource SecondaryButtonStyle}"
                                                    Margin="2"/>
                                            <Button Content="Scan" 
                                                    Click="ScanFolderButton_Click"
                                                    Tag="{Binding}"
                                                    Style="{DynamicResource SecondaryButtonStyle}"
                                                    Margin="2"/>
                                            <Button Content="Remove" 
                                                    Click="RemoveSingleFolderButton_Click"
                                                    Tag="{Binding}"
                                                    Style="{DynamicResource SecondaryButtonStyle}"
                                                    Margin="2"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <!-- Folder Sync Status -->
                    <TextBlock Grid.Row="2" Name="FolderSyncStatusText"
                               Text="No folders are being synced. Click 'Add Folder' to start syncing a folder."
                               HorizontalAlignment="Center" 
                               Margin="10"
                               Style="{DynamicResource SecondaryTextStyle}"/>
                </Grid>
            </TabItem>
            
            <TabItem Header="Status">
                <Grid Margin="10">
                    <StackPanel>
                        <TextBlock Name="StatusText" Text="Status: Ready" Style="{DynamicResource BodyTextStyle}" Margin="0,5"/>
                        <TextBlock Name="FilesCountText" Text="Files: 0" Style="{DynamicResource BodyTextStyle}" Margin="0,5"/>
                        <TextBlock Name="StorageText" Text="Storage: 0 bytes" Style="{DynamicResource BodyTextStyle}" Margin="0,5"/>
                        <TextBlock Name="BotInfoText" Text="Bot: Not connected" Style="{DynamicResource BodyTextStyle}" Margin="0,5"/>
                        <TextBlock Name="ChannelInfoText" Text="Channel: Not connected" Style="{DynamicResource BodyTextStyle}" Margin="0,5"/>
                        
                        <Separator Margin="0,10"/>
                        
                        <TextBlock Text="Recent Activity:" Style="{DynamicResource HeaderTextStyle}" Margin="0,10,0,5"/>
                        <ScrollViewer Height="200" VerticalScrollBarVisibility="Auto">
                            <TextBlock Name="ActivityLogText" 
                                       Text="No activity yet."
                                       TextWrapping="Wrap"
                                       FontFamily="Consolas"
                                       FontSize="12"
                                       Foreground="{DynamicResource SuccessBrush}"/>
                        </ScrollViewer>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2" Style="{DynamicResource ModernStatusBarStyle}">
            <StatusBarItem>
                <TextBlock Text="Ready"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="SecureCloud v1.0"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>